// BACKEND 
 - npm init -y
 - npm i typescript -D
 - npx tsc --init
 - npm i tsx -D ( Para rodar o app - melhor performance )
 - npm i tsup -D

 - npm i express
 - npm i bcrypt 
 - npm i cors 
 - npm i dotenv
 - npm i express-validator
 - npm i jsonwebtoken 
 - npm i mongoose 
 - npm i multer

// FRONTEND 


// SCRIPTS DEV E BUILD  

 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx src/app.ts",
    "dev:watch": "tsx watch src/app.ts",
    "build": "tsup src"
  }